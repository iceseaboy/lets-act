# Server-side only. Never put the provider key in the iOS app.
OPENAI_API_KEY=
# Choose a structured-output-capable, cost-efficient model available to your account.
OPENAI_MODEL=gpt-4.1-mini
# A private pilot token of at least 32 characters. Generate with openssl rand -hex 32.
PARSER_ACCESS_TOKEN=
PORT=8787
HOST=127.0.0.1
